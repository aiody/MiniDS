syntax = "proto3";
package Protocol;

enum CreatureState
{
	CREATURE_STATE_IDLE = 0;
	CREATURE_STATE_MOVING = 1;
	CREATURE_STATE_ATTACK = 2;
	CREATURE_STATE_DEAD = 3;
}

enum MoveDir
{
	MOVE_DIR_NONE = 0;
	MOVE_DIR_UP = 1;
	MOVE_DIR_DOWN = 2;
	MOVE_DIR_LEFT = 3;
	MOVE_DIR_RIGHT = 4;
}
